PORT=5000
NODE_ENV=development
JWT_SECRET=jntua_clms_super_secret_jwt_key_2024_anantapur
JWT_EXPIRES_IN=24h

# Database Configuration
# Supported values: 'sqlite' (zero-config local file) or 'mysql' (standard MySQL server)
DB_CLIENT=sqlite
SQLITE_DB_PATH=./database/jntua_clms.db

# MySQL Server Configuration (used when DB_CLIENT=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=jntua_clms

# Library Rules & Policies
BORROW_DAYS_LIMIT=15
FINE_PER_DAY=1
LOST_DAMAGED_CHARGE=300
STUDENT_MAX_BOOKS=3
FACULTY_MAX_BOOKS=6
